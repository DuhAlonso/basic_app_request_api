import 'package:flutter/material.dart';

class ApiAdvanced extends StatelessWidget {
  const ApiAdvanced({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
